<?xml version="1.0" encoding="UTF-8"?>

<Context>
    <!-- 커넥션풀 (DataSource객체) 리소스 설정  -->
    <Resource 
    	name="jdbc/oracle"
    	auth="Container"
    	type="javax.sql.DataSource"
    	driverClassName="oracle.jdbc.OracleDriver"
    	url="jdbc:oracle:thin:@localhost:1521:XE"
    	username="scot"
    	password="1234"
    	maxActive="50"
    	maxWait="-1"
    />
    <!-- 
    	톰캣 실행시 커넥션풀(DataSource객체)을 설정할때 연결할 데이터베이스 정보 저장 해 놓는다.
    	<Resource /> 태그의 속성들
    	
    	name = DataSource커넥션풀객체를  DAO.java파일에서 	불러와 사용할 JDNI 이름 설정
    	auth = 인증 주체 서버 
    	type = "데이터베이스 종류에 관한 커넥션풀이름"
    	driverClassName = 연결한 데이터베이스 종류에 따른 드라이버 클래스가 저장된 전체 경로
    	url = 접속할 데이터베이스 IP주소와 PORT번호 및 SID
    	username = 데이터베이스 접속 ID
    	password = 데이터베이스 접속 비밀번호
    	maxAcvive = 커넥션 풀 DataSource 풀 공간에 DB와 미리연결을 맺은 접속T4CConnection객체 생성 수 
    	maxWait = 새로운 요청의 접속이 생길때 까지 기다릴 수 있는 최대 시간 
     -->
</Context>













